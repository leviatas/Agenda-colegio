# Sólo para `npm run dev`. En Docker estos valores llegan por build.args (ver
# client/Dockerfile) y quedan horneados en el bundle: no hay configuración de
# runtime para la SPA.
VITE_GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
VITE_API_URL="http://localhost:4000"
